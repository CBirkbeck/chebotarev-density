{"id": "bp-links-2026-05-29", "timestamp": "2026-05-29T06:36:28Z", "command": "teach", "type": "user_teaching", "before_code": "", "after_code": "", "pattern_tags": ["leanblueprint", "dep_graph_links", "doc-gen4", "latexmk", "checkdecls", "ci", "leanok"], "description": "Getting a leanblueprint to deploy with ALL links resolving (Lean dep-graph links + LaTeX cross-refs). (1) dep-graph 'Lean' links point to <pages-url>/docs/find/#doc/<Name> (doc-gen4); they 404 unless CI builds API docs — set `api-docs: true` in the docgen-action step. (2) \\uses{...} expands to \\vphantom\\ref{...}; the dep-graph + PDF cross-refs need >=3 latex passes to converge. latexmk's default 2 passes leaves many 'undefined reference' warnings and CI fails — in blueprint/src/latexmkrc set `$max_repeat = 5;` and `$force_mode = 1;` (force_mode keeps emitting the PDF past transient unresolved-ref warnings). (3) The ACTUAL CI failure is usually a LaTeX parse error mid-file (undefined macro like \\re, or `\\delta_\\inf` where \\inf is a \\mathop) that aborts pdflatex so the .aux never populates, cascading into dozens of misleading 'undefined reference' warnings. Scan the log for the FIRST real error (`! Undefined control sequence`, `Missing { inserted`), not the ref warnings. Define every macro in macros/common.tex (\\re := \\operatorname{Re}); use explicit subscript braces (\\delta_{\\inf}). (4) `leanblueprint checkdecls` reads blueprint/lean_decls, which is REGENERATED by `leanblueprint web` — after renaming a Lean decl, `rm -rf blueprint/web blueprint/lean_decls; leanblueprint web` BEFORE checkdecls, else it reports stale 'X is missing'. (5) gitignore generated artifacts: blueprint/web/, blueprint/lean_decls, blueprint/src/*.paux (and *.aux/*.log/*.toc/*.pdf under src/). (6) Local pre-push loop: `leanblueprint pdf` then `grep -c undefined blueprint/print/print.log` must be 0 (after the passes); `leanblueprint web` then `leanblueprint checkdecls` must exit 0. (7) \\leanok goes on a theorem/proof whose Lean proof BODY is sorry-free (even if it calls a sorried lemma); the dep-graph then colors that node by its own status while the leaves stay open.", "math_area": "other", "accepted": true, "source": "user_teaching", "context": {"file_path": "", "theorem_name": "", "project": "chebotarev-density"}}
{"id": "use-le-not-ge-2026-06-02", "timestamp": "2026-06-02T19:40:00Z", "command": "teach", "type": "user_teaching", "before_code": "theorem primeIdealZetaSum_ge_log_minus_bounded : ... primeIdealZetaSum K univ s ≥ Real.log (1/(s-1)) - C", "after_code": "theorem log_minus_bounded_le_primeIdealZetaSum : ... Real.log (1/(s-1)) - C ≤ primeIdealZetaSum K univ s", "pattern_tags": ["use_le_not_ge", "inequality_orientation", "mathlib_convention", "style"], "description": "Always use ≤, never ≥ (and < not >) in Lean statements and hypotheses. Orient every inequality with the smaller side on the LEFT, and name such lemmas a_le_b not b_ge_a (e.g. log_minus_bounded_le_primeIdealZetaSum, not primeIdealZetaSum_ge_log_minus_bounded). mathlib convention, flagged by Riccardo Brasca on PR #4 (Density.lean). /cleanup's style audit did NOT catch a ≥ in a theorem statement/hypothesis, so this teaching adds the rule for future cleanup passes. Math prose inside docstrings may keep ≥ / 'k ≥ 2' where it reads naturally; the rule is about Lean code.", "math_area": "order", "accepted": true, "source": "user_teaching", "context": {"file_path": "CebotarevDensity/Density.lean", "theorem_name": "primeIdealZetaSum_ge_log_minus_bounded", "project": "chebotarev-density"}}
{"id": "teach-cleanup-docstring-prose-100col", "timestamp": "2026-06-04T19:00:00Z", "command": "teach", "type": "user_teaching", "before_code": "", "after_code": "", "pattern_tags": ["line_packing", "docstring_reflow", "cleanup", "style"], "description": "When /cleanup runs LINE PACKING it must also reflow docstring AND comment PROSE to fill ~100 columns, not just signature/code lines. The current LINE PACKING gate (audit item 6) is signature-only, which let under-filled (~70-col) docstrings in Density.lean through unchanged until reviewer riccardobrasca flagged them manually on PR #6 ('Do the same everywhere else in the file'). Mathlib fills docstring/comment prose to ~100 cols, not ~70. Fill every prose line to ~100 cols (merge under-wrapped lines), never exceed 100 display columns (unicode like 𝓞 σ 𝔭 ℝ counts as ~1 col, not byte length), and keep code-spans, math-display lines, */- bullet structure, and source-citation parentheticals atomic (never split mid-span). Complementary per-file style check to the existing signature LINE PACKING gate.", "math_area": "other", "accepted": true, "source": "user_teaching", "context": {"file_path": "", "theorem_name": "", "project": "chebotarev-density"}}
{"id":"golf-after-sorryfiller-dedup","timestamp":"2026-06-05","command":"cleanup","type":"style_correction","description":"sorry-filler-deep agents produce WORKING but DUPLICATED/long proof code: the cyclotomic bridge had the per-term tail bound (||-Log(1-w)-w|| <= Np^-2 + the ||w||<=1/2 step) proved TWICE (inline in hRbdd and again in hsumG). A golf pass hoisted it to a shared hterm/hsumtail, -47 lines. ALWAYS golf/cleanup agent-generated proofs before committing -- do not defer to pre-PR.","math_area":"analytic_number_theory","accepted":true,"source":"user_teaching"}
{"id":"simplify-lean-nlinarith-false-positive","timestamp":"2026-06-05","command":"simplify","type":"style_correction","description":"/simplify's efficiency review agent flags nlinarith->linarith on Lean goals it calls 'purely linear' that are actually NONLINEAR (products): e.g. ||w||^2 <= ||w|| and M*||s-2|| <= M*1 (product of M>=0 and a bound). linarith cannot form products; nlinarith is required. VERIFY each /simplify efficiency finding against the actual goal before applying -- the agent's 'linear' judgement is unreliable for norm/product goals.","math_area":"other","accepted":true,"source":"user_teaching"}
{"id":"cleanup-parallel-samefile-race","timestamp":"2026-06-05","command":"cleanup","type":"workflow","description":"Phase-4 cleanup workers dispatched in PARALLEL on the SAME file can race: a worker whose build-gate fails because of ANOTHER concurrent worker's in-progress (broken) edit will 'fix' code OUTSIDE its own decl to unblock its gate. Worse, lake's cache-REPLAY can mask the broken intermediate state (baseline build shows green via replay, not a real recompile). Mitigations: (1) run same-file Phase-4 workers SEQUENTIALLY, or batch by non-adjacent decls; (2) ALWAYS verify the final state with a genuine fresh compile `lake env lean <file>` (bypasses .olean cache) before trusting a green `lake build`. Caught a worker trying a non-existent `NumberField.HeightOneSpectrum.one_lt_absNorm` (real name is under RingOfIntegers and is deprecated).","math_area":"other","accepted":true,"source":"user_teaching"}
