# Inventory: `CebotarevDensity/NumberFieldEulerProduct.lean`

File header: `module` (lines 1–15 imports); module docstring (17–26); `@[expose] public section` (28); `noncomputable section` (30); opens `NumberField`, scoped `Topology nonZeroDivisors` (32–33). Namespace `Chebotarev` (39); section `NumberFieldEulerProduct` (41) with `variable (L : Type*) [Field L] [NumberField L]` (43). Total 995 lines.

---

### `instance instFintypeSym`
- **Type** `(α : Type*) [Finite α] (n : ℕ) : Fintype (Sym α n)` · **What** Equips the `n`-th symmetric power `Sym α n` of a finite type with a `Fintype` instance. · **How** `Fintype.ofFinite (Sym α n)` — converts the (derivable) `Finite (Sym α n)` to a noncomputable `Fintype`. · **Hypotheses** `α` finite. · **Uses from project** [] · **Used by** `summable_tsum_symGeometric`, `tsum_symGeometric` (via `Fintype.card (Sym α n)`). · **Visibility** public instance (noncomputable). · **Lines** 35–37. · **Notes** Declared at top level, before `namespace Chebotarev`.

### `abbrev NonzeroIdeal`
- **Type** `: Type _ := {I : Ideal (𝓞 L) // I ≠ ⊥}` · **What** The subtype of nonzero ideals of the ring of integers `𝓞 L`. · **How** Definitional subtype abbreviation. · **Hypotheses** `L` a number field (from `variable`). · **Uses from project** [] · **Used by** Pervasive: `idealNormMultiplicity`, `idealNormMultiplicity_*`, `dedekindZeta_eq_tsum_idealNormMultiplicity`, `sum_idealNormMultiplicity_isBigO`, `hasSum_nonzeroIdeal_absNorm_cpow`, `dedekindZeta_eq_tprod_primeIdeal`, `weighted_eulerProduct_eq_tsum`, `primeFactorsOf`, etc. · **Visibility** public abbrev. · **Lines** 45. · **Notes** —

### `def idealNormMultiplicity`
- **Type** `(n : ℕ) : ℕ := Nat.card {I : NonzeroIdeal L // Ideal.absNorm I.1 = n}` · **What** The number of nonzero ideals of `𝓞 L` with absolute norm exactly `n` (the Dedekind-zeta Dirichlet coefficient `a_n`). · **How** `Nat.card` of the subtype of nonzero ideals with `absNorm = n`. · **Hypotheses** `L` number field. · **Uses from project** [`NonzeroIdeal`] · **Used by** `idealNormMultiplicity_zero/one/mul`, `dedekindZeta_eq_tsum_idealNormMultiplicity`, `summable_idealNormMultiplicity_mul_cpow_neg`, `dedekindZeta_eq_tprod_primePowerSeries`, `sum_idealNormMultiplicity_isBigO`, `hasSum_nonzeroIdeal_absNorm_cpow`, `dedekindZeta_re_pos_of_one_lt`. · **Visibility** public def (noncomputable). · **Lines** 47–48. · **Notes** —

### `lemma idealNormMultiplicity_zero`
- **Type** `: idealNormMultiplicity L 0 = 0` · **What** No nonzero ideal has norm 0. · **How** Unfold; `Nat.card_eq_zero` reduces to showing the subtype is empty (`Or.inl`); a nonzero ideal with `absNorm = 0` contradicts `Ideal.absNorm_eq_zero_iff` (norm 0 ⟺ ideal = ⊥). · **Hypotheses** none beyond `L`. · **Uses from project** [`idealNormMultiplicity`] · **Used by** `idealNormMultiplicity_mul`, `dedekindZeta_eq_tsum_idealNormMultiplicity`, `summable_idealNormMultiplicity_mul_cpow_neg`, `dedekindZeta_eq_tprod_primePowerSeries`; also `Density.lean`. · **Visibility** public lemma. · **Lines** 50–53. · **Notes** —

### `lemma idealNormMultiplicity_one`
- **Type** `: idealNormMultiplicity L 1 = 1` · **What** Exactly one nonzero ideal (namely `⊤ = (1)`) has norm 1. · **How** Builds a `Unique` instance on the subtype: `default` is `⊤` (with `Ideal.absNorm_top`), uniqueness via `Ideal.absNorm_eq_one_iff` (norm 1 ⟺ ideal = ⊤), nested `Subtype.ext`; concludes by `Nat.card_unique`. · **Hypotheses** none beyond `L`. · **Uses from project** [`idealNormMultiplicity`] · **Used by** `idealNormMultiplicity_mul` (extensively), `dedekindZeta_eq_tprod_primePowerSeries`, `dedekindZeta_re_pos_of_one_lt`. · **Visibility** public lemma. · **Lines** 55–61. · **Notes** —

### `lemma idealNormMultiplicity_mul`
- **Type** `{m n : ℕ} (hcop : Nat.Coprime m n) : idealNormMultiplicity L (m * n) = idealNormMultiplicity L m * idealNormMultiplicity L n` · **What** The ideal-norm counting function is multiplicative on coprime arguments (CRT for ideals of given norm). · **How** Handles `m = 0`/`n = 0` and the `m ≤ 1`/`n ≤ 1` corners via `interval_cases` + `idealNormMultiplicity_one`. The main case (`1 &lt; m, 1 &lt; n`) builds an explicit `Equiv` between `{absNorm = m*n}` and `{absNorm = m} × {absNorm = n}`: forward map sends `I ↦ (I ⊔ (m), I ⊔ (n))`, backward sends `(J, L') ↦ J·L'`. Coprimality `(m)⊔(n)=⊤` comes from `Ideal.isCoprime_span_singleton_iff` + `Nat.Coprime.isCoprime`. Key sub-facts (all `have`s): `h_mul_sup` ((I⊔(m))(I⊔(n)) = I⊔(mn)), `h_sup_absNorm`, `h_abs_span_nat` (`absNorm (r) = r^finrank` via `Algebra.norm_algebraMap_of_basis`), `h_fwd_norms` (the split norms are exactly `m`,`n`, proved by divisibility + `nlinarith`), `h_cop_*`/`h_inv_*` (inverse identities via `IsCoprime`). Concludes `Nat.card_congr h_equiv` + `Nat.card_prod`. · **Hypotheses** `m, n` coprime naturals. · **Uses from project** [`idealNormMultiplicity`, `idealNormMultiplicity_zero`, `idealNormMultiplicity_one`] · **Used by** `dedekindZeta_eq_tprod_primePowerSeries` (`hf_mul`). · **Visibility** public lemma. · **Lines** 63–212 (≈150 lines). · **Notes** &gt;30 lines; the file's largest proof. Uses `ac_rfl`, `nlinarith`, `interval_cases`, `Algebra.norm_algebraMap_of_basis (Module.finBasis ℤ (𝓞 L))`.

### `lemma dedekindZeta_eq_tsum_idealNormMultiplicity`
- **Type** `{s : ℂ} (hs : 1 &lt; s.re) : NumberField.dedekindZeta L s = ∑' n : ℕ, (idealNormMultiplicity L n : ℂ) * (n : ℂ) ^ (-s)` · **What** Rewrites the Dedekind zeta `L`-series as the Dirichlet series `Σ a_n n^{-s}` with `a_n = idealNormMultiplicity`. · **How** Unfolds `dedekindZeta`/`LSeries`/`LSeries.term` and does a `tsum_congr` term-by-term. For `n = 0`: both sides 0 via `Complex.zero_cpow` (needs `s ≠ 0`). For `n &gt; 0`: rewrites `cpow_neg`, `div_eq_mul_inv`, then identifies the coefficient count via an `Equiv` between `{Ideal // absNorm = n}` and `{NonzeroIdeal // absNorm = n}` (nonzero because `absNorm_bot` ≠ `n&gt;0`), closed by `Nat.card_congr`. · **Hypotheses** `1 &lt; Re s`. · **Uses from project** [`idealNormMultiplicity`, `idealNormMultiplicity_zero`, `NonzeroIdeal`] · **Used by** `dedekindZeta_eq_tprod_primePowerSeries`, `hasSum_nonzeroIdeal_absNorm_cpow`, `dedekindZeta_re_pos_of_one_lt`; also `ZetaProduct.lean` (line 3211). · **Visibility** public lemma. · **Lines** 214–237. · **Notes** Uses `lia` (the project's `omega`-style closer).

### `lemma summable_tsum_symGeometric`
- **Type** `(α : Type*) [Fintype α] [Finite α] {z : ℂ} (hz : ‖z‖ &lt; 1) : Summable (fun n =&gt; (Fintype.card (Sym α n) : ℂ) * z ^ n) ∧ (∑' n, (Fintype.card (Sym α n) : ℂ) * z ^ n) = ((1 - z)⁻¹) ^ Fintype.card α` · **What** The "symmetric-power generating function": `Σ_n #(Sym α n) z^n = (1-z)^{-#α}` (multi-set / negative-binomial identity), with summability. · **How** Case `#α = 0`: `α` empty, every `n&gt;0` term vanishes (`Nat.multichoose_zero_succ`), finite support, sum is the `n=0` term = 1 = `(...)^0`. Case `#α = k+1`: rewrites `#(Sym α n) = (n+k).choose k` via `Sym.card_sym_eq_choose` + `Nat.choose_symm_add`, then transfers to mathlib's `summable_choose_mul_geometric_of_norm_lt_one` and `tsum_choose_mul_geometric_of_norm_lt_one`. · **Hypotheses** `α` finite, `‖z‖ &lt; 1`. · **Uses from project** [`instFintypeSym` (instance, implicit)] · **Used by** `tsum_symGeometric`. · **Visibility** public lemma. · **Lines** 239–261 (&gt;30 lines incl. signature). · **Notes** &gt;30 lines. Key mathlib: `Sym.card_sym_eq_multichoose`, `Sym.card_sym_eq_choose`.

### `lemma tsum_symGeometric`
- **Type** `(α : Type*) [Fintype α] [Finite α] {z : ℂ} (hz : ‖z‖ &lt; 1) : (∑' n, (Fintype.card (Sym α n) : ℂ) * z ^ n) = ((1 - z)⁻¹) ^ Fintype.card α` · **What** The value half of `summable_tsum_symGeometric` (drops the summability conjunct). · **How** `(summable_tsum_symGeometric α hz).2`. · **Hypotheses** as above. · **Uses from project** [`summable_tsum_symGeometric`] · **Used by** unused in file. · **Visibility** public lemma. · **Lines** 263–265. · **Notes** Not referenced elsewhere in repo grep — likely consumed downstream or kept as convenience API.

### `lemma sum_idealNormMultiplicity_isBigO`
- **Type** `: (fun n : ℕ =&gt; ∑ k ∈ Finset.Icc 1 n, (idealNormMultiplicity L k : ℝ)) =O[Filter.atTop] (fun n : ℕ =&gt; (n : ℝ) ^ (1 : ℝ))` · **What** Partial sums of the ideal-norm counting function grow like `O(n)` — i.e. `#{nonzero ideals with norm ≤ n} = O(n)`. · **How** `h_finite`: each norm-fibre is finite (`Ideal.finite_setOf_absNorm_eq`). `h_sum_card`: the partial sum equals `Nat.card {NonzeroIdeal // absNorm ≤ n}` via `Finset.card_preimage_eq_sum_card_image_eq` and identifying the preimage of `Icc 1 n` with `{absNorm ≤ n}` (lower bound 1 because nonzero). `h_card_bridge`: re-casts the subtype to `(Ideal (𝓞 L))⁰` (`mem_nonZeroDivisors_*`). Then applies `Asymptotics.isBigO_atTop_natCast_rpow_of_tendsto_div_rpow` fed by `NumberField.Ideal.tendsto_norm_le_div_atTop₀` composed with `tendsto_natCast_atTop_atTop`, matched up with `filter_upwards` + `push_cast`. · **Hypotheses** none beyond `L`. · **Uses from project** [`idealNormMultiplicity`, `NonzeroIdeal`] · **Used by** `summable_idealNormMultiplicity_mul_cpow_neg`; also `ZetaProduct.lean` (line 2579). · **Visibility** public lemma. · **Lines** 270–305 (&gt;30 lines), with explanatory docstring 267–269. · **Notes** &gt;30 lines. The arithmetic-geometry input is `tendsto_norm_le_div_atTop₀`.

### `lemma summable_idealNormMultiplicity_mul_cpow_neg`
- **Type** `{s : ℂ} (hs : 1 &lt; s.re) : Summable fun n : ℕ =&gt; ‖(idealNormMultiplicity L n : ℂ) * (n : ℂ) ^ (-s)‖` · **What** Absolute convergence of the Dedekind Dirichlet series for `Re s &gt; 1`. · **How** Obtains `LSeriesSummable` of the real coefficient sequence via `LSeriesSummable_of_sum_norm_bigO_and_nonneg` (fed `sum_idealNormMultiplicity_isBigO`, nonnegativity, growth exponent `1 &lt; Re s`). Then `h_term_eq` rewrites `LSeries.term` into the `a_n · n^{-s}` form (`split_ifs` on `n=0`), and takes `.norm` of the resulting `Summable`. · **Hypotheses** `1 &lt; Re s`. · **Uses from project** [`idealNormMultiplicity`, `idealNormMultiplicity_zero`, `sum_idealNormMultiplicity_isBigO`] · **Used by** `dedekindZeta_eq_tprod_primePowerSeries`, `hasSum_nonzeroIdeal_absNorm_cpow`, `dedekindZeta_re_pos_of_one_lt`; also `Density.lean` (131) and `ZetaProduct.lean` (3208). · **Visibility** public lemma. · **Lines** 307–324. · **Notes** Key mathlib: `LSeriesSummable_of_sum_norm_bigO_and_nonneg`.

### `lemma dedekindZeta_eq_tprod_primePowerSeries`
- **Type** `{s : ℂ} (hs : 1 &lt; s.re) : NumberField.dedekindZeta L s = ∏' q : Nat.Primes, (∑' k : ℕ, (idealNormMultiplicity L ((q:ℕ)^k) : ℂ) * (((q:ℕ)^k : ℕ) : ℂ) ^ (-s))` · **What** Euler product of `ζ_L` over rational primes `q`, each factor being the local Dirichlet sub-series in powers of `q`. · **How** Sets `f n = a_n n^{-s}`; establishes `f 0 = 0`, `f 1 = 1`, multiplicativity `f(mn)=f m·f n` on coprimes (via `idealNormMultiplicity_mul` + `Complex.natCast_mul_natCast_cpow`), and norm-summability (from `summable_idealNormMultiplicity_mul_cpow_neg`). Rewrites the zeta as `Σ f n` then applies `EulerProduct.eulerProduct_tprod`. · **Hypotheses** `1 &lt; Re s`. · **Uses from project** [`idealNormMultiplicity`, `idealNormMultiplicity_zero`, `idealNormMultiplicity_one`, `idealNormMultiplicity_mul`, `dedekindZeta_eq_tsum_idealNormMultiplicity`, `summable_idealNormMultiplicity_mul_cpow_neg`] · **Used by** unused in file. · **Visibility** public lemma. · **Lines** 326–340. · **Notes** Core mathlib hook: `EulerProduct.eulerProduct_tprod`.

### `def insertPiEquiv` (private)
- **Type** `{ι : Type*} [DecidableEq ι] (a : ι) (s : Finset ι) (ha : a ∉ s) : ({i // i ∈ insert a s} → ℕ) ≃ ℕ × ({i // i ∈ s} → ℕ)` · **What** Re-indexes an exponent vector over `insert a s` as the pair (value at `a`, restriction to `s`). · **How** Explicit `Equiv`: `toFun f = (f a, f|_s)`; `invFun (n,g) i = if i = a then n else g …` (using `Finset.mem_insert.mp …` to land in `s`); `left_inv`/`right_inv` by `funext`/`ext` + `by_cases i = a`. · **Hypotheses** `DecidableEq ι`, `a ∉ s`. · **Uses from project** [] · **Used by** `insertPiEquiv_fst`, `insertPiEquiv_snd`, `prodInsertAttach`, `finsetGeometricProd_summable_and_hasSum`. · **Visibility** private noncomputable def. · **Lines** 350–369, with docstring 349. · **Notes** Helper introduced by the §"Sub-lemmas" block (342–347).

### `lemma insertPiEquiv_fst` (private, `@[simp]`)
- **Type** `… (e : {i // i ∈ insert a s} → ℕ) : (insertPiEquiv a s ha e).1 = e ⟨a, Finset.mem_insert_self a s⟩` · **What** First component of `insertPiEquiv` is the value at `a`. · **How** `rfl`. · **Hypotheses** `a ∉ s`. · **Uses from project** [`insertPiEquiv`] · **Used by** `prodInsertAttach`. · **Visibility** private `@[simp]` lemma. · **Lines** 371–373. · **Notes** —

### `lemma insertPiEquiv_snd` (private, `@[simp]`)
- **Type** `… (i : {i // i ∈ s}) : (insertPiEquiv a s ha e).2 i = e ⟨i.1, Finset.mem_insert_of_mem i.2⟩` · **What** Second component of `insertPiEquiv` restricts `e` to `s`. · **How** `rfl`. · **Hypotheses** `a ∉ s`. · **Uses from project** [`insertPiEquiv`] · **Used by** `prodInsertAttach`. · **Visibility** private `@[simp]` lemma. · **Lines** 375–377. · **Notes** —

### `lemma prodInsertAttach` (private)
- **Type** `{ι} [DecidableEq ι] (g : ι → ℂ) (a : ι) (s : Finset ι) (ha : a ∉ s) (e : {i // i ∈ insert a s} → ℕ) : ∏ i ∈ (insert a s).attach, g i.1 ^ e i = g a ^ (insertPiEquiv a s ha e).1 * ∏ i ∈ s.attach, g i.1 ^ (insertPiEquiv a s ha e).2 i` · **What** Splits the attached product over `insert a s` into the factor at `a` times the product over `s`. · **How** Rewrites both components of `insertPiEquiv` (via `insertPiEquiv_fst/snd`), then `Finset.attach_insert`, `Finset.prod_insert`, `Finset.prod_image`; the image-injectivity side goals discharged by `Subtype.ext`/`Subtype.mk.inj` and the non-membership goal from `ha`. · **Hypotheses** `a ∉ s`. · **Uses from project** [`insertPiEquiv`, `insertPiEquiv_fst`, `insertPiEquiv_snd`] · **Used by** `finsetGeometricProd_summable_and_hasSum`. · **Visibility** private lemma. · **Lines** 381–393, docstring 379–380. · **Notes** —

### `lemma finsetGeometricProd_summable_and_hasSum` (private)
- **Type** `{ι} (g : ι → ℂ) (hg : ∀ i, ‖g i‖ &lt; 1) (s : Finset ι) : (Summable fun e : {i // i ∈ s} → ℕ =&gt; ‖∏ i ∈ s.attach, g i.1 ^ e i‖) ∧ HasSum (fun e =&gt; ∏ i ∈ s.attach, g i.1 ^ e i) (∏ i ∈ s, (1 - g i)⁻¹)` · **What** Combinatorial heart of the finite Euler-factor identity: the finite product of geometric factors `∏(1-g i)⁻¹` is the norm-summable `tsum`/`HasSum` over exponent vectors of `∏ g i^{e i}`. · **How** `Finset.induction` on `s`. Empty: the exponent type is over `∅`, the product is constant `1` (`Finset.attach_empty`), summable + `HasSum` via `hasSum_unique`. Insert `a`: `Finset.prod_insert ha`; uses `hasSum_geometric_of_norm_lt_one` for the new factor, `summable_geometric_of_lt_one` for its norm, then `summable_mul_of_summable_norm` / `Summable.mul_norm` / `HasSum.mul` to multiply the IH (a `HasSum` over `s`-vectors) with the geometric series over `ℕ`. Finally re-indexes `ℕ × ({i∈s}→ℕ)` to `{i ∈ insert a s}→ℕ` via `insertPiEquiv` (using `prodInsertAttach`, `Equiv.summable_iff`, `Equiv.hasSum_iff`). · **Hypotheses** `‖g i‖ &lt; 1` for all `i`. · **Uses from project** [`insertPiEquiv`, `prodInsertAttach`] · **Used by** `prod_eulerFactor_eq_tsum_exponentVector`, `weighted_prod_eulerFactor_eq_tsum`. · **Visibility** private lemma. · **Lines** 408–464 (&gt;30 lines), docstring 406–407. · **Notes** &gt;30 lines. Key mathlib: `hasSum_geometric_of_norm_lt_one`, `HasSum.mul`, `Summable.mul_norm`.

### `lemma prod_natCast_cpow` (private)
- **Type** `{ι} (S : Finset ι) (m : ι → ℕ) (z : ℂ) : (∏ i ∈ S, (m i : ℂ) ^ z) = ((∏ i ∈ S, m i : ℕ) : ℂ) ^ z` · **What** A product of natural-number cpow's equals the cpow of the product (a cpow-distributes-over-natCast-product identity). · **How** `Finset.induction`; empty is `simp`; insert uses `Finset.prod_insert`, `Nat.cast_mul`, and `Complex.natCast_mul_natCast_cpow` (the key fact that `(ab)^z = a^z b^z` for natural `a,b ≥ 0`). · **Hypotheses** none. · **Uses from project** [] · **Used by** `prod_absNorm_cpow_eq_absNorm_prod_pow_cpow`. · **Visibility** private lemma. · **Lines** 467–474, docstring 466. · **Notes** —

### `lemma norm_absNorm_cpow_neg_lt_one`
- **Type** `{s : ℂ} (hs : 1 &lt; s.re) (𝔭 : {𝔭 : Ideal (𝓞 L) // 𝔭.IsPrime ∧ 𝔭 ≠ ⊥}) : ‖(Ideal.absNorm 𝔭.1 : ℂ) ^ (-s)‖ &lt; 1` · **What** For `Re s &gt; 1`, a nonzero prime ideal has `‖N𝔭^{-s}‖ &lt; 1` (since `N𝔭 ≥ 2`). · **How** Norm `≠ 0` (`absNorm_eq_zero_iff` + nonzero) and `≠ 1` (`absNorm_eq_one_iff` + prime ⟹ not top), so `N𝔭 ≥ 2` (`lia`). Rewrites norm via `Complex.norm_natCast_cpow_of_pos`, `Complex.neg_re`, then `Real.rpow_lt_one_of_one_lt_of_neg` with base `&gt; 1` and exponent `-Re s &lt; 0`. · **Hypotheses** `1 &lt; Re s`; `𝔭` prime and nonzero. · **Uses from project** [] (only mathlib + `lia`) · **Used by** `prod_eulerFactor_eq_tsum_exponentVector`, `weighted_prod_eulerFactor_eq_tsum`. · **Visibility** public lemma. · **Lines** 477–486, docstring 476. · **Notes** Uses `lia`.

### `theorem prod_eulerFactor_eq_tsum_exponentVector`
- **Type** `{s : ℂ} (hs : 1 &lt; s.re) (S : Finset {𝔭 // 𝔭.IsPrime ∧ 𝔭 ≠ ⊥}) : (∏ 𝔭 ∈ S, (1 - (Ideal.absNorm 𝔭.1 : ℂ)^(-s))⁻¹) = ∑' e : S →₀ ℕ, ∏ 𝔭 ∈ S.attach, (Ideal.absNorm 𝔭.1.1 : ℂ)^(-(e 𝔭 : ℂ)*s)` · **What** Finite Euler-factor identity (Sharifi Prop. 7.1.9 for ideals): the finite product of prime Euler factors equals the sum over exponent vectors `e : S →₀ ℕ`. · **How** Gets `‖N𝔭^{-s}‖ &lt; 1` from `norm_absNorm_cpow_neg_lt_one`; takes the `HasSum` from `finsetGeometricProd_summable_and_hasSum`; rewrites each summand `(N𝔭^{-s})^{e𝔭}` to `N𝔭^{-(e𝔭)s}` (`Complex.cpow_nat_mul` + `ring_nf`); re-indexes the `tsum` from `S → ℕ` to `S →₀ ℕ` via `Finsupp.equivFunOnFinite`. · **Hypotheses** `1 &lt; Re s`. · **Uses from project** [`norm_absNorm_cpow_neg_lt_one`, `finsetGeometricProd_summable_and_hasSum`] · **Used by** `dedekindZeta_eq_tprod_primeIdeal` (`hpartial`). · **Visibility** public theorem. · **Lines** 492–511, docstring 488–491. · **Notes** Math context block (Sharifi 7.1.9 quote) at 395–404.

### `theorem absNorm_prod_pow_of_primeIdeal`
- **Type** `(S : Finset {𝔭 // …}) (e : S → ℕ) : Ideal.absNorm (∏ 𝔭 ∈ S.attach, 𝔭.1.1 ^ e 𝔭) = ∏ 𝔭 ∈ S.attach, Ideal.absNorm 𝔭.1.1 ^ e 𝔭` · **What** `N(∏ 𝔭^{e𝔭}) = ∏ (N𝔭)^{e𝔭}` — `absNorm` is multiplicative over the prime-power product. · **How** `map_prod` (absNorm is a monoid hom) then `map_pow` on each factor via `Finset.prod_congr`. · **Hypotheses** none. · **Uses from project** [] · **Used by** `prod_absNorm_cpow_eq_absNorm_prod_pow_cpow`. · **Visibility** public theorem. · **Lines** 514–519, docstring 513. · **Notes** —

### `theorem prod_absNorm_cpow_eq_absNorm_prod_pow_cpow`
- **Type** `{s : ℂ} (S : Finset {𝔭 // …}) (e : S → ℕ) : (∏ 𝔭 ∈ S.attach, (Ideal.absNorm 𝔭.1.1 : ℂ)^(-(e 𝔭 : ℂ)*s)) = (Ideal.absNorm (∏ 𝔭 ∈ S.attach, 𝔭.1.1 ^ e 𝔭) : ℂ)^(-s)` · **What** `∏ N𝔭^{-(e𝔭)s} = N(∏ 𝔭^{e𝔭})^{-s}` — collapses the per-prime cpow product to a single ideal-norm cpow. · **How** `hinner`: each factor `N𝔭^{-(e𝔭)s} = (N𝔭^{e𝔭})^{-s}` via `Nat.cast_pow` + `Complex.natCast_cpow_natCast_mul` + `ring_nf`. Then `Finset.prod_congr`, `prod_natCast_cpow` (pull product inside cpow), `absNorm_prod_pow_of_primeIdeal`. · **Hypotheses** none. · **Uses from project** [`prod_natCast_cpow`, `absNorm_prod_pow_of_primeIdeal`] · **Used by** `dedekindZeta_eq_tprod_primeIdeal` (`hpartial`), `weighted_prod_eulerFactor_eq_tsum` (`hsummand`). · **Visibility** public theorem. · **Lines** 522–531, docstring 521. · **Notes** —

### `theorem hasSum_nonzeroIdeal_absNorm_cpow`
- **Type** `{s : ℂ} (hs : 1 &lt; s.re) : HasSum (fun I : NonzeroIdeal L =&gt; (Ideal.absNorm I.1 : ℂ)^(-s)) (NumberField.dedekindZeta L s)` · **What** `Σ_𝔞 N𝔞^{-s} = ζ_L(s)` over nonzero ideals, as a `HasSum`. · **How** Partitions the ideal sum by norm via `e := Equiv.sigmaFiberEquiv (absNorm ·)`. `hfiber`: each norm-fibre is finite (`Ideal.finite_setOf_absNorm_eq`). `hfiber_norm`/`hfiber_val`: the (constant) sum over a fibre of norm `n` is `idealNormMultiplicity · ‖n^{-s}‖` resp. `· n^{-s}` (`tsum_const`, `nsmul_eq_mul`; `n=0` fibre empty). `hsummable`: norm-summability via `e.summable_iff` + `summable_sigma_of_nonneg` + per-fibre finiteness + `summable_idealNormMultiplicity_mul_cpow_neg`. `hval`: rewrites the tsum through `dedekindZeta_eq_tsum_idealNormMultiplicity` and `hsummable_sigma.tsum_sigma`. Concludes `hsummable.of_norm.hasSum`. · **Hypotheses** `1 &lt; Re s`. · **Uses from project** [`NonzeroIdeal`, `idealNormMultiplicity`, `summable_idealNormMultiplicity_mul_cpow_neg`, `dedekindZeta_eq_tsum_idealNormMultiplicity`] · **Used by** `dedekindZeta_eq_tprod_primeIdeal` (`hsumD`), `weighted_eulerProduct_eq_tsum` (`hnormD`); also `ZetaProduct.lean` (2601, 2905, 2960, 3397). · **Visibility** public theorem. · **Lines** 547–595 (&gt;30 lines), docstring 544–546 (flags it as public because `ZetaProduct.lean` needs the ideal-sum form). · **Notes** &gt;30 lines. Key mathlib: `Equiv.sigmaFiberEquiv`, `summable_sigma_of_nonneg`, `tsum_sigma`.

### `theorem factorization_primePow_apply` (private)
- **Type** `(𝔭 𝔮 : {𝔭 // 𝔭.IsPrime ∧ 𝔭 ≠ ⊥}) (n : ℕ) : factorization (𝔭.1 ^ n) 𝔮.1 = if 𝔮 = 𝔭 then n else 0` (under `open UniqueFactorizationMonoid`) · **What** The exponent of `𝔮` in the UFM-factorisation of `𝔭^n` is `n` if `𝔮 = 𝔭`, else `0`. · **How** `factorization_pow`, `Finsupp.smul_apply`, `factorization_eq_count`, `normalizedFactors_irreducible` (using `Ideal.prime_of_isPrime …`.irreducible), `normalize_eq`, `Multiset.count_singleton`; `split_ifs` with the two prime-equality matches resolved by `Subtype.ext`/`congrArg Subtype.val`. · **Hypotheses** `𝔭, 𝔮` prime nonzero. · **Uses from project** [] · **Used by** `factorization_prod_primePow_eq`. · **Visibility** private theorem. · **Lines** 599–609, docstring 598, `open … in` 597. · **Notes** —

### `theorem factorization_prod_primePow_apply` (private)
- **Type** `(S : Finset {…}) (e : {…} → ℕ) (𝔮 : {…}) : factorization (∏ 𝔭 ∈ S.attach, 𝔭.1.1 ^ e 𝔭.1) 𝔮.1 = ∑ 𝔭 ∈ S.attach, factorization (𝔭.1.1 ^ e 𝔭.1) 𝔮.1` · **What** Factorisation exponent of `𝔮` in a product of prime powers is the sum of the per-factor exponents. · **How** `Finset.induction` on `S`; empty via `factorization_one`/`Finsupp.coe_zero`; insert via `Finset.attach_insert`, `Finset.prod_insert`, `factorization_mul` (factors nonzero: `pow_ne_zero` + `Finset.prod_ne_zero_iff`), `Finsupp.add_apply`, `Finset.prod_image`/`Finset.sum_image`, IH; image-non-membership side goals from `ha` + `Subtype.mk.inj`. · **Hypotheses** none beyond types. · **Uses from project** [] · **Used by** `factorization_prod_primePow_eq`. · **Visibility** private theorem. · **Lines** 613–634 (&gt;30 lines), docstring 612, `open … in` 611. · **Notes** &gt;30 lines.

### `theorem factorization_prod_primePow_eq` (private)
- **Type** `(S : Finset {…}) (e : {…} → ℕ) (𝔮 : {…}) : factorization (∏ 𝔭 ∈ S.attach, 𝔭.1.1 ^ e 𝔭.1) 𝔮.1 = if 𝔮 ∈ S then e 𝔮 else 0` · **What** Exponent of `𝔮` in `∏_{𝔭∈S} 𝔭^{e𝔭}` is `e 𝔮` when `𝔮 ∈ S`, else `0` (used to prove injectivity of `idealOfExp`). · **How** Rewrites by `factorization_prod_primePow_apply` then `factorization_primePow_apply`; `by_cases 𝔮 ∈ S`. If yes, `Finset.sum_eq_single ⟨𝔮,…⟩` isolates the `𝔮` term. If no, `Finset.sum_eq_zero` since every `factorization_primePow_apply` term is `if 𝔮 = b then … = 0`. · **Hypotheses** none beyond types. · **Uses from project** [`factorization_prod_primePow_apply`, `factorization_primePow_apply`] · **Used by** `dedekindZeta_eq_tprod_primeIdeal` (`hinj`), `weighted_eulerProduct_eq_tsum` (`hinj`). · **Visibility** private theorem. · **Lines** 638–654, docstring 637, `open … in` 636. · **Notes** —

### `def primeFactorsOf` (private)
- **Type** `(𝔞 : NonzeroIdeal L) : Finset {𝔭 : Ideal (𝓞 L) // 𝔭.IsPrime ∧ 𝔭 ≠ ⊥}` · **What** The normalized prime factors of `𝔞` packaged as a `Finset` of (the subtype of) nonzero prime ideals. · **How** Takes `(normalizedFactors 𝔞.1).toFinset.attach` and `Finset.map`s through an embedding that lifts each normalized factor `p` to the subtype using `Ideal.isPrime_of_prime`/`prime_of_normalized_factor` (primeness) and `.ne_zero` (nonzero); the embedding's injectivity is `Subtype.ext ∘ congrArg`. · **Hypotheses** `𝔞 ≠ ⊥`. · **Uses from project** [`NonzeroIdeal`] · **Used by** `mem_primeFactorsOf`, `dedekindZeta_eq_tprod_primeIdeal` (`F.biUnion (primeFactorsOf L)`), `weighted_eulerProduct_eq_tsum` (same). · **Visibility** private noncomputable def, `open UniqueFactorizationMonoid in`. · **Lines** 658–669, docstring 657, `open … in` 656. · **Notes** —

### `theorem mem_primeFactorsOf` (private)
- **Type** `(𝔞 : NonzeroIdeal L) (p : Ideal (𝓞 L)) (hp : p ∈ normalizedFactors 𝔞.1) : ∃ 𝔭 ∈ primeFactorsOf L 𝔞, 𝔭.1 = p` · **What** Every normalized prime factor `p` of `𝔞` is realised by some element of `primeFactorsOf 𝔞`. · **How** Provides the witness `⟨p, ⟨prime, ne_zero⟩⟩` (`Ideal.isPrime_of_prime`/`prime_of_normalized_factor`), then membership via `primeFactorsOf`, `Finset.mem_map` and `Multiset.mem_toFinset`. · **Hypotheses** `p` a normalized factor of `𝔞`. · **Uses from project** [`NonzeroIdeal`, `primeFactorsOf`] · **Used by** `dedekindZeta_eq_tprod_primeIdeal`, `weighted_eulerProduct_eq_tsum` (both `hF_sub`). · **Visibility** private theorem, `open UniqueFactorizationMonoid in`. · **Lines** 673–679, docstring 672, `open … in` 671. · **Notes** —

### `theorem dedekindZeta_eq_tprod_primeIdeal`
- **Type** `{s : ℂ} (hs : 1 &lt; s.re) : NumberField.dedekindZeta L s = ∏' 𝔭 : {𝔭 // 𝔭.IsPrime ∧ 𝔭 ≠ ⊥}, (1 - (Ideal.absNorm 𝔭.1 : ℂ)^(-s))⁻¹` · **What** Prime-ideal Euler product (Sharifi Thm 7.1.12): `ζ_K(s) = ∏_𝔭 (1 - N𝔭^{-s})⁻¹` over nonzero primes, for `Re s &gt; 1`. · **How** Sets `D 𝔞 = N𝔞^{-s}` (HasSum to `ζ` via `hasSum_nonzeroIdeal_absNorm_cpow`, norm-summable). `idealOfExp S e = ∏ 𝔭^{e𝔭}`. `hinj`: injectivity from `factorization_prod_primePow_eq` recovering `e 𝔮` from the factorisation. `hmem`: every ideal whose factors all lie in `S` is in the range — reconstruct `𝔞` from its `normalizedFactors` counts via `Finsupp.onFinset`, `finprod_pow_count_eq_of_subsingleton_units`, `finprod_eq_finsetProd_of_mulSupport_subset`, `Finset.prod_subset`. `hpartial`: the finite product equals `∑_{range} D` via `prod_eulerFactor_eq_tsum_exponentVector`, `prod_absNorm_cpow_eq_absNorm_prod_pow_cpow`, `tsum_range`. Then the `S ↑ ⊤` limit: `HasProd.tprod_eq`, `SummationFilter.unconditional`, `Metric.tendsto_atTop`; the tail bound uses `tendsto_tsum_compl_atTop_zero`, the cofinal `F.biUnion (primeFactorsOf L)`, splitting the ideal sum (`tsum_subtype_add_tsum_subtype_compl`), `norm_tsum_le_tsum_norm`, and `tsum_le_tsum_of_inj`. · **Hypotheses** `1 &lt; Re s`. · **Uses from project** [`NonzeroIdeal`, `hasSum_nonzeroIdeal_absNorm_cpow`, `factorization_prod_primePow_eq`, `prod_eulerFactor_eq_tsum_exponentVector`, `prod_absNorm_cpow_eq_absNorm_prod_pow_cpow`, `primeFactorsOf`, `mem_primeFactorsOf`] · **Used by** unused in file; consumed in `Density.lean` (482) and `ZetaProduct.lean` (2929). · **Visibility** public theorem, `open UniqueFactorizationMonoid in`. · **Lines** 684–774 (≈90 lines, &gt;30), docstring 682–683, math block 533–542. · **Notes** &gt;30 lines. Major mathlib: `tendsto_tsum_compl_atTop_zero`, `finprod_pow_count_eq_of_subsingleton_units`, `Summable.tsum_subtype_add_tsum_subtype_compl`, `tsum_le_tsum_of_inj`.

### `theorem weight_prod_primePow` (private)
- **Type** `(w : Ideal (𝓞 L) → ℂ) (hw_one : w ⊤ = 1) (hw_mul : ∀ {𝔞 𝔟}, 𝔞 ≠ ⊥ → 𝔟 ≠ ⊥ → w (𝔞*𝔟) = w 𝔞 * w 𝔟) (S : Finset {…}) (e : S → ℕ) : w (∏ 𝔭 ∈ S.attach, 𝔭.1.1 ^ e 𝔭) = ∏ 𝔭 ∈ S.attach, (w 𝔭.1.1)^e 𝔭` (with `omit [NumberField L]`) · **What** A completely-multiplicative weight `w` distributes over a prime-power product. · **How** `wpow`: `w(𝔭^k) = (w 𝔭)^k` by induction on `k` (`pow_succ`, `hw_mul`). `key`: induction over a `Finset S` of subtypes (`Finset.induction`), using `hw_mul` to split the new factor (nonzero via `pow_ne_zero`/`Finset.prod_ne_zero_iff`) and `wpow`; applied to `S.attach`. · **Hypotheses** `w ⊤ = 1`; `w` multiplicative on nonzero ideals. · **Uses from project** [] · **Used by** `weighted_prod_eulerFactor_eq_tsum` (`hsummand`). · **Visibility** private theorem; `omit [NumberField L] in` (does not need the number-field instance). · **Lines** 789–808, docstring 786–788, `omit … in` 785. · **Notes** —

### `theorem weighted_prod_eulerFactor_eq_tsum` (private)
- **Type** `{s : ℂ} (hs : 1 &lt; s.re) (w) (hw_one) (hw_mul) (hw_norm : ∀ 𝔞, ‖w 𝔞‖ ≤ 1) (S) (idealOfExp : (S →₀ ℕ) → NonzeroIdeal L) (hidealOfExp : ∀ e, (idealOfExp e).1 = ∏ 𝔭 ∈ S.attach, 𝔭.1.1 ^ e 𝔭) (hinj : Injective idealOfExp) : (∏ 𝔭 ∈ S, (1 - w 𝔭.1 * N𝔭^{-s})⁻¹) = ∑' 𝔞 : Set.range idealOfExp, w 𝔞.1.1 * N𝔞.1.1^{-s}` · **What** Weighted finite Euler-factor identity: with ratio `g 𝔭 = w(𝔭)N𝔭^{-s}`, the finite product is the weighted Dirichlet partial sum over `S`-factored ideals. · **How** `hg`: `‖g 𝔭‖ &lt; 1` from `‖w 𝔭‖ ≤ 1` (`hw_norm`) times `norm_absNorm_cpow_neg_lt_one`. Takes the `HasSum` from `finsetGeometricProd_summable_and_hasSum` for `g`. `hsummand`: identifies the exponent-vector summand with `w(𝔞)N𝔞^{-s}` via `mul_pow`, `Finset.prod_mul_distrib`, `prod_absNorm_cpow_eq_absNorm_prod_pow_cpow`, `weight_prod_primePow`, `hidealOfExp`. Re-indexes `S → ℕ` to `S →₀ ℕ` (`Finsupp.equivFunOnFinite`, `HasSum.congr_fun`), then `tsum_range` with `hinj`. · **Hypotheses** `1 &lt; Re s`; `w` completely multiplicative with `‖w‖ ≤ 1`; `idealOfExp` the prime-power realiser, injective. · **Uses from project** [`NonzeroIdeal`, `norm_absNorm_cpow_neg_lt_one`, `finsetGeometricProd_summable_and_hasSum`, `prod_absNorm_cpow_eq_absNorm_prod_pow_cpow`, `weight_prod_primePow`] · **Used by** `weighted_eulerProduct_eq_tsum` (`hpartial`). · **Visibility** private theorem. · **Lines** 813–858 (&gt;30 lines), docstring 810–812. · **Notes** &gt;30 lines.

### `theorem weighted_eulerProduct_eq_tsum`
- **Type** `{s : ℂ} (hs : 1 &lt; s.re) (w) (hw_one : w ⊤ = 1) (hw_mul) (hw_norm : ∀ 𝔞, ‖w 𝔞‖ ≤ 1) : (∏' 𝔭 : {𝔭 // 𝔭.IsPrime ∧ 𝔭 ≠ ⊥}, (1 - w 𝔭.1 * N𝔭^{-s})⁻¹) = ∑' 𝔞 : NonzeroIdeal L, w 𝔞.1 * N𝔞.1^{-s}` · **What** Weighted prime-ideal Euler product (Sharifi 7.1.18): the χ-twisted analogue of `dedekindZeta_eq_tprod_primeIdeal`. With `w = 1` it specialises to that theorem; with `w = galoisCharacterOnIdeal K L χ` it powers the abelian Euler product. · **How** Structurally mirrors `dedekindZeta_eq_tprod_primeIdeal` with `Dw 𝔞 = w 𝔞.1 N𝔞^{-s}`. `hnormD`: norm-summable by comparison `‖Dw‖ ≤ ‖N𝔞^{-s}‖` (`hw_norm`) against `hasSum_nonzeroIdeal_absNorm_cpow`. `hinj`/`hmem` identical to the unweighted proof (`factorization_prod_primePow_eq`; reconstruction via `Finsupp.onFinset`, `finprod_pow_count_eq_of_subsingleton_units`). `hpartial` delegates to `weighted_prod_eulerFactor_eq_tsum`. The `S ↑ ⊤` limit is the same `HasProd`/`tendsto_atTop` tail-bound argument (`tendsto_tsum_compl_atTop_zero`, `F.biUnion (primeFactorsOf L)`, split + `tsum_le_tsum_of_inj`). · **Hypotheses** `1 &lt; Re s`; `w ⊤ = 1`; `w` multiplicative on nonzero ideals; `‖w 𝔞‖ ≤ 1`. · **Uses from project** [`NonzeroIdeal`, `hasSum_nonzeroIdeal_absNorm_cpow`, `factorization_prod_primePow_eq`, `primeFactorsOf`, `mem_primeFactorsOf`, `weighted_prod_eulerFactor_eq_tsum`] · **Used by** unused in file; consumed in `ZetaProduct.lean` (291, with `w = galoisCharacterOnIdeal K L χ`). · **Visibility** public theorem, `open UniqueFactorizationMonoid in`. · **Lines** 865–961 (≈95 lines, &gt;30), docstring 861–864, math block 776–783. · **Notes** &gt;30 lines. `hinj`/`hmem` are near-verbatim duplicates of those in `dedekindZeta_eq_tprod_primeIdeal`.

### `theorem dedekindZeta_re_pos_of_one_lt`
- **Type** `(s : ℝ) (hs : 1 &lt; s) : 0 &lt; (NumberField.dedekindZeta L (s : ℂ)).re` · **What** For real `s &gt; 1`, the real part of `ζ_K(s)` is strictly positive (Sharifi Def 7.1.11). · **How** Defines real `g n = a_n · n^{-s}`. `key`: each complex term equals `↑(g n)` (`Complex.ofReal_cpow`, `push_cast`). `hsumC` (complex summable from `summable_idealNormMultiplicity_mul_cpow_neg`) gives `hsumR` (real summable via `Complex.summable_ofReal`). `hre`: `Re(ζ) = Σ g n` (`dedekindZeta_eq_tsum_idealNormMultiplicity`, `Complex.re_tsum`). Concludes by `Summable.tsum_pos`: terms `≥ 0` (`mul_nonneg`, `Real.rpow_nonneg`) and the `n=1` term `&gt; 0` (`idealNormMultiplicity_one`). · **Hypotheses** real `1 &lt; s`. · **Uses from project** [`idealNormMultiplicity`, `idealNormMultiplicity_one`, `summable_idealNormMultiplicity_mul_cpow_neg`, `dedekindZeta_eq_tsum_idealNormMultiplicity`] · **Used by** unused in file; consumed in `ZetaProduct.lean` (3302, 3398, 3502). · **Visibility** public theorem. · **Lines** 964–990 (&gt;30 lines), docstring 963. · **Notes** &gt;30 lines. Key mathlib: `Summable.tsum_pos`, `Complex.re_tsum`, `Complex.summable_ofReal`.

---

## File Summary

**Totals.** 25 declarations: 1 top-level `Fintype` instance, 1 abbrev (`NonzeroIdeal`), 2 `def`s (`idealNormMultiplicity`, `primeFactorsOf`) + 1 private helper def (`insertPiEquiv`); the rest lemmas/theorems. **8 are private** (`insertPiEquiv`, `insertPiEquiv_fst`, `insertPiEquiv_snd`, `prodInsertAttach`, `finsetGeometricProd_summable_and_hasSum`, `prod_natCast_cpow`, `factorization_primePow_apply`, `factorization_prod_primePow_apply`, `factorization_prod_primePow_eq`, `primeFactorsOf`, `mem_primeFactorsOf`, `weight_prod_primePow`, `weighted_prod_eulerFactor_eq_tsum` — 13 private in fact). **No `sorry`. No `axiom`. No `set_option`.** Two attribute modifiers: `@[expose]` on the public section, `@[simp]` on the two `insertPiEquiv_*` lemmas; one `omit [NumberField L]` (on `weight_prod_primePow`); `open UniqueFactorizationMonoid in` on 7 declarations.

**Key internal API used by ≥3 in-file declarations.**
- `idealNormMultiplicity` (def) — used by 8 declarations.
- `NonzeroIdeal` (abbrev) — pervasive.
- `summable_idealNormMultiplicity_mul_cpow_neg` — used by `dedekindZeta_eq_tprod_primePowerSeries`, `hasSum_nonzeroIdeal_absNorm_cpow`, `dedekindZeta_re_pos_of_one_lt` (3).
- `dedekindZeta_eq_tsum_idealNormMultiplicity` — used by `dedekindZeta_eq_tprod_primePowerSeries`, `hasSum_nonzeroIdeal_absNorm_cpow`, `dedekindZeta_re_pos_of_one_lt` (3).
- `idealNormMultiplicity_one` — used by `idealNormMultiplicity_mul`, `dedekindZeta_eq_tprod_primePowerSeries`, `dedekindZeta_re_pos_of_one_lt` (3).
- `prod_absNorm_cpow_eq_absNorm_prod_pow_cpow`, `norm_absNorm_cpow_neg_lt_one`, `finsetGeometricProd_summable_and_hasSum`, `primeFactorsOf`, `mem_primeFactorsOf`, `factorization_prod_primePow_eq`, `hasSum_nonzeroIdeal_absNorm_cpow` — each used by 2 (the two Euler-product theorems / their finite-`S` helpers).

**Key mathlib API leaned on (load-bearing).** `EulerProduct.eulerProduct_tprod`; `Ideal.absNorm` (`_eq_zero_iff`, `_eq_one_iff`, `_top`, `_span_singleton`, `finite_setOf_absNorm_eq`); `NumberField.Ideal.tendsto_norm_le_div_atTop₀`; `Asymptotics.isBigO_atTop_natCast_rpow_of_tendsto_div_rpow`; `LSeriesSummable_of_sum_norm_bigO_and_nonneg`; `Sym.card_sym_eq_choose`/`_multichoose` + `summable_choose_mul_geometric_of_norm_lt_one`; `Equiv.sigmaFiberEquiv`, `summable_sigma_of_nonneg`; `tendsto_tsum_compl_atTop_zero`, `finprod_pow_count_eq_of_subsingleton_units`, `tsum_le_tsum_of_inj`; `UniqueFactorizationMonoid.{factorization, normalizedFactors, normalize_eq}`.

**Unused-in-file (public surface for downstream).** `tsum_symGeometric`, `dedekindZeta_eq_tprod_primePowerSeries`, `dedekindZeta_eq_tprod_primeIdeal`, `weighted_eulerProduct_eq_tsum`, `dedekindZeta_re_pos_of_one_lt`. All are terminal exports — none has an in-file consumer.

**`sorry` / `set_option` / `&gt;30-line` lists.**
- sorry: none. set_option: none.
- &gt;30 lines (11): `idealNormMultiplicity_mul` (≈150), `dedekindZeta_eq_tprod_primeIdeal` (≈90), `weighted_eulerProduct_eq_tsum` (≈95), `hasSum_nonzeroIdeal_absNorm_cpow` (≈48), `sum_idealNormMultiplicity_isBigO` (≈36), `finsetGeometricProd_summable_and_hasSum` (≈56), `weighted_prod_eulerFactor_eq_tsum` (≈45), `dedekindZeta_re_pos_of_one_lt` (≈27 body but ≈30+ with sig), `factorization_prod_primePow_apply` (≈22), `summable_tsum_symGeometric` (≈23). (Several others are borderline ~20–25 lines.)

**Cross-file consumers (confirmed by grep, per the docstrings' claims).**
- `Density.lean`: `idealNormMultiplicity` (+ `_zero`), `summable_idealNormMultiplicity_mul_cpow_neg`, `dedekindZeta_eq_tprod_primeIdeal`.
- `ZetaProduct.lean` (the heaviest consumer): `idealNormMultiplicity`, `sum_idealNormMultiplicity_isBigO`, `summable_idealNormMultiplicity_mul_cpow_neg`, `dedekindZeta_eq_tsum_idealNormMultiplicity`, `hasSum_nonzeroIdeal_absNorm_cpow` (4 sites), `dedekindZeta_eq_tprod_primeIdeal`, `weighted_eulerProduct_eq_tsum` (with `w = galoisCharacterOnIdeal K L χ`, the Sharifi 7.1.18 χ-twist), `dedekindZeta_re_pos_of_one_lt` (3 sites).

This file is the analytic engine room: it proves the unweighted and χ-weighted prime-ideal Euler products (`dedekindZeta_eq_tprod_primeIdeal`, `weighted_eulerProduct_eq_tsum`), the ideal-sum form of ζ (`hasSum_nonzeroIdeal_absNorm_cpow`), positivity (`dedekindZeta_re_pos_of_one_lt`), and the multiplicative/asymptotic facts about the ideal-norm counting function — all sorry-free and axiom-free.</result>
<usage><subagent_tokens>55794</subagent_tokens><tool_uses>3</tool_uses><duration_ms>210575</duration_ms></usage>
</task-notification>